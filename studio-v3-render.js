'use strict';

import {themeById,buildLyricCues,drawProcedural,drawVisualizer,drawLyrics,drawIntroOutro} from './studio-v3-themes.js';
import {sceneAtTime} from './studio-v3-story.js';

const sleep=ms=>new Promise(r=>setTimeout(r,ms));
const finite=(v,d=0)=>Number.isFinite(Number(v))?Number(v):d;
const unit=v=>Math.max(0,Math.min(1,finite(v,0)));
const AC=()=>{const C=window.AudioContext||window.webkitAudioContext;if(!C)throw new Error('Web Audio is not supported in this browser.');return new C()};

function bands(freq){if(!freq?.length)return {low:.12,mid:.1,high:.08,energy:.1};const avg=(a,b)=>{const s=Math.max(0,Math.floor(a)),e=Math.min(freq.length,Math.max(s+1,Math.ceil(b)));let n=0,sum=0;for(let i=s;i<e;i++){sum+=finite(freq[i],0)/255;n++}return unit(n?sum/n:0)};const low=avg(1,freq.length*.08),mid=avg(freq.length*.08,freq.length*.34),high=avg(freq.length*.34,freq.length*.82);return {low,mid,high,energy:unit(low*.48+mid*.34+high*.18)}}
class BeatGate{constructor(){this.avg=.12;this.last=-99;this.count=0;this.fx=null;this.fxUntil=0}update(b,time,theme){const t=finite(time,0),low=unit(b.low);this.avg=this.avg*.94+low*.06;const threshold=Math.max(.34,this.avg*1.32+.06),cooldown=Math.max(.30,theme.cutMin*.42),hit=low>threshold&&t-this.last>cooldown;if(hit){this.last=t;this.count++;const modes=theme.id.includes('naru')?['line','flash','glitch']:theme.id==='phonk-noir'?['glitch','flash','zoom']:['zoom','line','flash'];this.fx=modes[this.count%modes.length];this.fxUntil=t+.14}return {hit,accent:hit&&low>.62,fx:t<this.fxUntil?this.fx:null}}reset(){this.avg=.12;this.last=-99;this.count=0;this.fx=null;this.fxUntil=0}}
function cover(ctx,src,W,H,zoom=1,dx=0,dy=0){const iw=finite(src?.videoWidth||src?.width||src?.naturalWidth,0),ih=finite(src?.videoHeight||src?.height||src?.naturalHeight,0);if(!iw||!ih)return;const r=Math.max(W/iw,H/ih)*Math.max(.2,finite(zoom,1)),dw=iw*r,dh=ih*r;try{ctx.drawImage(src,(W-dw)/2+finite(dx,0)*W,(H-dh)/2+finite(dy,0)*H,dw,dh)}catch{}}

/* One decode lane is critical on mobile. New scene requests replace the desired
 * target; they never start a second 4K image/video decode in parallel. */
class SceneLease{
  constructor(library,{maxW=1280,maxH=720}={}){this.library=library;this.maxW=maxW;this.maxH=maxH;this.current=null;this.desired=null;this.pump=null;this.closed=false}
  request(item){this.desired=item||null;if(this.closed)return Promise.resolve(null);if(!this.pump)this.pump=this._pump().finally(()=>{this.pump=null;if(!this.closed&&this.desired?.id!==this.current?.id)this.request(this.desired)});return this.pump}
  async load(item){return this.request(item)}
  async _decode(item){
    if(!item)return null;
    if(item.kind==='video'&&item.source==='remote'){
      const v=document.createElement('video');v.crossOrigin='anonymous';v.src=item.remoteUrl;v.muted=true;v.loop=true;v.playsInline=true;v.preload='metadata';v.disablePictureInPicture=true;
      await new Promise((res,rej)=>{let done=false;const finish=fn=>{if(done)return;done=true;v.onloadeddata=null;v.oncanplay=null;v.onerror=null;fn()};v.onloadeddata=()=>finish(res);v.oncanplay=()=>finish(res);v.onerror=()=>finish(()=>rej(new Error('A cloud stock clip could not be streamed.')));try{v.load()}catch{}setTimeout(()=>finish(res),4500)});try{await v.play()}catch{}
      return {id:item.id,kind:'video',source:v,release(){try{v.pause();v.removeAttribute('src');v.load()}catch{}}};
    }
    const file=await this.library.open(item);
    if(item.kind==='image'){
      let bitmap;try{bitmap=await createImageBitmap(file,{imageOrientation:'from-image',resizeWidth:this.maxW,resizeHeight:this.maxH,resizeQuality:'medium'})}catch{bitmap=await createImageBitmap(file)}
      return {id:item.id,kind:'image',source:bitmap,release(){try{bitmap.close()}catch{}}};
    }
    const url=URL.createObjectURL(file),v=document.createElement('video');v.src=url;v.muted=true;v.loop=true;v.playsInline=true;v.preload='metadata';v.disablePictureInPicture=true;
    try{
      await new Promise((res,rej)=>{let done=false;const finish=fn=>{if(done)return;done=true;v.onloadeddata=null;v.oncanplay=null;v.onerror=null;fn()};v.onloadeddata=()=>finish(res);v.oncanplay=()=>finish(res);v.onerror=()=>finish(()=>rej(new Error('A selected video could not be decoded.')));try{v.load()}catch{}setTimeout(()=>finish(res),3000)});try{await v.play()}catch{}
      return {id:item.id,kind:'video',source:v,release(){try{v.pause();v.removeAttribute('src');v.load();URL.revokeObjectURL(url)}catch{}}};
    }catch(e){try{URL.revokeObjectURL(url)}catch{}throw e}
  }
  async _pump(){
    while(!this.closed){const wanted=this.desired;if(!wanted){this.current?.release?.();this.current=null;return null}if(this.current?.id===wanted.id)return this.current;let next=null;try{next=await this._decode(wanted)}catch(e){if(this.desired?.id===wanted.id)this.desired=null;throw e}if(this.closed){next?.release?.();return null}if(this.desired?.id!==wanted.id){next?.release?.();await sleep(0);continue}this.current?.release?.();this.current=next;return next}return null
  }
  clear(){this.desired=null;this.current?.release?.();this.current=null}
  close(){this.closed=true;this.desired=null;this.current?.release?.();this.current=null}
}

function chooseMime(){
  const supported=x=>typeof MediaRecorder!=='undefined'&&MediaRecorder.isTypeSupported?.(x),ua=typeof navigator!=='undefined'?navigator.userAgent||'':'',safari=/Safari/i.test(ua)&&!/Chrome|Chromium|Android/i.test(ua);
  const stable=safari?['video/mp4;codecs=avc1.42E01E,mp4a.40.2','video/mp4','video/webm;codecs=vp8,opus','video/webm']:['video/webm;codecs=vp8,opus','video/webm','video/webm;codecs=vp9,opus','video/mp4;codecs=avc1.42E01E,mp4a.40.2','video/mp4'];
  return stable.find(supported)||''
}
async function opfsWriter(name){if(!navigator.storage?.getDirectory)return null;try{const root=await navigator.storage.getDirectory(),h=await root.getFileHandle(name,{create:true}),w=await h.createWritable();return {root,h,w,name}}catch{return null}}
async function finishOpfs(x){if(!x)return null;await x.w.close();return x.h.getFile()}
function storyMotion(scene,b){if(!scene)return {zoom:1+unit(b.low)*.018,dx:0,dy:0};const p=unit(scene.progress),energy=unit(scene.energy),motion=scene.motion||'drift';let zoom=1.015+energy*.025,dx=0,dy=0;if(motion==='push')zoom+=p*(.06+energy*.035);else if(motion==='pull')zoom+=(1-p)*.06;else if(motion==='track')dx=(p-.5)*.06;else if(motion==='handheld'){dx=Math.sin(p*31)*.006*(.4+energy);dy=Math.cos(p*27)*.004*(.4+energy)}else if(motion==='drift')dx=(p-.5)*.025;return {zoom,dx,dy}}
function storyTransition(ctx,W,H,scene){if(!scene)return;const p=unit(scene.progress),edge=Math.min(p,1-p),type=scene.transition||'cut';let a=0;if(type==='fade'||type==='dissolve')a=edge<.10?(1-edge/.10)*.52:0;else if(type==='flash')a=p<.035?(1-p/.035)*.25:0;else if(type==='whip')a=p<.06?(1-p/.06)*.18:0;if(a>0){ctx.fillStyle=type==='flash'?`rgba(255,255,255,${a})`:`rgba(0,0,0,${a})`;ctx.fillRect(0,0,W,H)}}

export class RenderEngine{
  constructor(canvas,library,{onState=()=>{}}={}){this.canvas=canvas;this.ctx=canvas.getContext('2d',{alpha:false});this.library=library;this.onState=onState;this.sequence=[];this.sceneIndex=0;this.storyIndex=-1;this.storyScene=null;this.lastMediaId='';this.lastCut=-99;this.beat=new BeatGate();this.lease=new SceneLease(library);this.preview=null;this.previewRaf=0;this.rendering=false;this.abortFlag=false;this.gestureCtx=null;this.renderAudio=null;this.renderAudioUrl=''}
  quality(aspect='landscape'){
    const mem=Number(navigator.deviceMemory)||4,cores=Number(navigator.hardwareConcurrency)||4,mobile=/Android|iPhone|iPad|Mobile/i.test(navigator.userAgent||'')||innerWidth<700,weak=mem<=2||cores<=2;
    if(aspect==='vertical'){if(weak)return {w:480,h:854,fps:24,bitrate:1_700_000,tier:'emergency'};return mobile||mem<=4?{w:540,h:960,fps:24,bitrate:2_300_000,tier:'mobile'}:{w:720,h:1280,fps:30,bitrate:4_000_000,tier:'desktop'}}
    if(weak)return {w:854,h:480,fps:24,bitrate:1_700_000,tier:'emergency'};return mobile||mem<=4?{w:960,h:540,fps:24,bitrate:2_300_000,tier:'mobile'}:{w:1280,h:720,fps:30,bitrate:4_800_000,tier:'desktop'}
  }
  resetSequence(project){this.sequence=this.library.chooseSequence(18,project.title||project.idea||'ridge');this.sceneIndex=0;this.storyIndex=-1;this.storyScene=null;this.lastMediaId='';this.lastCut=-99;this.beat.reset();this.lease.clear();const first=project.scenePlan?.scenes?.[0],item=first?this.library.chooseForScene(first,{seed:project.title||'ridge'}):this.sequence[0];if(item){this.lastMediaId=item.id;this.lease.request(item).catch(()=>{})}}
  async prepareUserGesture(audioFile=null){this.stopPreview(true);try{if(!this.gestureCtx)this.gestureCtx=AC();await this.gestureCtx.resume()}catch{}if(audioFile){this._releaseRenderAudio();const a=new Audio(),url=URL.createObjectURL(audioFile);a.src=url;a.preload='metadata';a.playsInline=true;a.volume=0;try{await a.play();a.pause();a.currentTime=0}catch{}a.volume=1;this.renderAudio=a;this.renderAudioUrl=url}return true}
  _releaseRenderAudio(){if(this.renderAudio)try{this.renderAudio.pause();this.renderAudio.removeAttribute('src');this.renderAudio.load()}catch{}if(this.renderAudioUrl)try{URL.revokeObjectURL(this.renderAudioUrl)}catch{}this.renderAudio=null;this.renderAudioUrl=''}
  _syncStory(time,project){const scene=sceneAtTime(time,project.scenePlan);if(!scene)return null;if(scene.index!==this.storyIndex){this.storyIndex=scene.index;this.storyScene=scene;const item=this.library.chooseForScene(scene,{excludeId:this.lastMediaId,seed:project.title||'ridge'});if(item){this.lastMediaId=item.id;this.lease.request(item).catch(e=>this.onState({type:'warn',message:e.message}))}}else this.storyScene=scene;return this.storyScene}
  _maybeCut(time,b,beat,theme,project){if(project.scenePlan?.scenes?.length)return;if(!this.sequence.length)return;const min=Math.max(.58,theme.cutMin),strong=beat.accent||b.energy>.58;if((beat.hit||time-this.lastCut>min*2.4)&&time-this.lastCut>=min&&(strong||time-this.lastCut>min*1.65)){this.lastCut=time;this.sceneIndex=(this.sceneIndex+1)%this.sequence.length;const item=this.sequence[this.sceneIndex];if(item){this.lastMediaId=item.id;this.lease.request(item).catch(e=>this.onState({type:'warn',message:e.message}))}}}
  drawFrame(time,duration,b,project){const q={w:this.canvas.width,h:this.canvas.height},theme=themeById(project.theme),beat=this.beat.update(b,time,theme),story=this._syncStory(time,project);this._maybeCut(time,b,beat,theme,project);const ctx=this.ctx,W=q.w,H=q.h,src=this.lease.current?.source,m=storyMotion(story,b),zoom=m.zoom+(beat.fx==='zoom'?.035:0)+unit(b.low)*.008;ctx.save();ctx.fillStyle=theme.palette[2];ctx.fillRect(0,0,W,H);if(src){cover(ctx,src,W,H,zoom,m.dx,m.dy);ctx.fillStyle='rgba(0,0,0,.18)';ctx.fillRect(0,0,W,H)}else drawProcedural(ctx,W,H,time,b,theme);drawVisualizer(ctx,W,H,time,b,theme);if(beat.fx==='flash'){ctx.fillStyle=`rgba(255,255,255,${unit(.06+theme.effect*.12)})`;ctx.fillRect(0,0,W,H)}if(beat.fx==='line'){ctx.strokeStyle=theme.palette[0];ctx.globalAlpha=.15;ctx.lineWidth=Math.max(2,W*.003);for(let i=0;i<7;i++){ctx.beginPath();ctx.moveTo(0,H*(.12+i*.11));ctx.lineTo(W,H*(.02+i*.11));ctx.stroke()}ctx.globalAlpha=1}if(beat.fx==='glitch'){ctx.globalCompositeOperation='screen';ctx.fillStyle=theme.palette[1];ctx.globalAlpha=.045;for(let i=0;i<4;i++)ctx.fillRect((i%2?1:-1)*W*.012,H*((i*23)%80)/100,W,H*.007);ctx.globalCompositeOperation='source-over';ctx.globalAlpha=1}storyTransition(ctx,W,H,story);drawLyrics(ctx,W,H,time,project.lyricCues||[],theme);drawIntroOutro(ctx,W,H,time,duration,project,theme);ctx.restore()}
  async startPreview(audioEl,project){if(!audioEl?.src)throw new Error('Choose a song first.');this.stopPreview(true);this.resetSequence(project);if(!this.preview){const ac=AC(),an=ac.createAnalyser();an.fftSize=1024;an.smoothingTimeConstant=.78;const src=ac.createMediaElementSource(audioEl);src.connect(an);an.connect(ac.destination);this.preview={ac,an,src,audioEl,freq:new Uint8Array(an.frequencyBinCount)}}else this.preview.audioEl=audioEl;await this.preview.ac.resume();await audioEl.play();const loop=()=>{if(audioEl.paused)return;this.preview.an.getByteFrequencyData(this.preview.freq);this.drawFrame(audioEl.currentTime,audioEl.duration||120,bands(this.preview.freq),project);this.previewRaf=requestAnimationFrame(loop)};loop()}
  stopPreview(pauseAudio=false){cancelAnimationFrame(this.previewRaf);this.previewRaf=0;if(pauseAudio&&this.preview?.audioEl){try{this.preview.audioEl.pause()}catch{}try{this.preview.ac?.suspend()}catch{}}}
  abort(){this.abortFlag=true}
  async render(audioFile,project,{onProgress=()=>{}}={}){
    if(this.rendering)throw new Error('A render is already running.');if(!audioFile)throw new Error('Choose a song first.');this.rendering=true;this.abortFlag=false;this.stopPreview(true);const q=this.quality(project.aspect||'landscape');this.canvas.width=q.w;this.canvas.height=q.h;this.lease.maxW=q.w;this.lease.maxH=q.h;this.resetSequence(project);let ac=null,audio=null,url='',ownsUrl=false,stream=null,raf=0,writer=null,chunks=[],chunkBytes=0,pendingBytes=0,visibilityHandler=null;
    try{
      const mime=chooseMime(),ext=mime.includes('mp4')?'mp4':'webm',name=`ridge-${String(project.title||'video').replace(/[^a-z0-9_-]+/gi,'-').slice(0,70)}-${Date.now()}.${ext}`;writer=await opfsWriter(name);if(this.renderAudio&&this.renderAudioUrl){audio=this.renderAudio;url=this.renderAudioUrl}else{audio=new Audio();url=URL.createObjectURL(audioFile);ownsUrl=true;audio.src=url;audio.preload='metadata';audio.playsInline=true}audio.volume=1;try{audio.currentTime=0}catch{}
      if(!Number.isFinite(audio.duration)||!audio.duration)await new Promise((res,rej)=>{let done=false;const finish=fn=>{if(done)return;done=true;audio.onloadedmetadata=null;audio.onerror=null;fn()};audio.onloadedmetadata=()=>finish(res);audio.onerror=()=>finish(()=>rej(new Error('The song could not be decoded by this browser.')));setTimeout(()=>finish(res),5000)});
      const duration=Number.isFinite(audio.duration)?audio.duration:0;if(!duration)throw new Error('Could not read song duration.');if(!project.lyricCues?.length)project.lyricCues=buildLyricCues(project.lyrics||'',duration);
      ac=AC();await ac.resume();const src=ac.createMediaElementSource(audio),an=ac.createAnalyser(),dest=ac.createMediaStreamDestination();an.fftSize=1024;an.smoothingTimeConstant=.76;src.connect(an);an.connect(dest);const canvasStream=this.canvas.captureStream(q.fps);stream=new MediaStream([...canvasStream.getVideoTracks(),...dest.stream.getAudioTracks()]);
      let rec;try{rec=new MediaRecorder(stream,mime?{mimeType:mime,videoBitsPerSecond:q.bitrate,audioBitsPerSecond:160000}:undefined)}catch{rec=new MediaRecorder(stream,{videoBitsPerSecond:Math.min(q.bitrate,2_300_000),audioBitsPerSecond:128000})}
      const freq=new Uint8Array(an.frequencyBinCount);let write=Promise.resolve(),writeError=null,ended=false;const stopped=new Promise((res,rej)=>{rec.onerror=e=>rej(e.error||new Error('MediaRecorder failed.'));rec.onstop=res;rec.ondataavailable=e=>{if(!e.data?.size)return;if(writer){pendingBytes+=e.data.size;if(pendingBytes>32*1024*1024){this.abortFlag=true;writeError=new Error('Storage writes are too slow on this device; Ridge stopped before memory could run away.')}const blob=e.data;write=write.then(async()=>{await writer.w.write(blob);pendingBytes=Math.max(0,pendingBytes-blob.size)}).catch(err=>writeError=err)}else{chunks.push(e.data);chunkBytes+=e.data.size;if(chunkBytes>280*1024*1024){this.abortFlag=true;writeError=new Error('This browser cannot stream the export to disk and the in-memory safety limit was reached.')}}}});
      audio.onended=()=>ended=true;visibilityHandler=()=>{if(document.hidden&&rec.state==='recording'){try{rec.pause();audio.pause();this.lease.current?.source?.pause?.()}catch{}}else if(!document.hidden&&rec.state==='paused'){try{rec.resume();audio.play().catch(()=>{});const p=this.lease.current?.source?.play?.();p?.catch?.(()=>{})}catch{}}};document.addEventListener('visibilitychange',visibilityHandler);
      an.getByteFrequencyData(freq);this.drawFrame(0,duration,bands(freq),project);rec.start(1500);try{await audio.play()}catch{throw new Error('Browser blocked render playback. Tap CREATE VIDEO once more; Ridge will reuse the granted media gesture.')}
      const loop=()=>{if(ended||this.abortFlag)return;an.getByteFrequencyData(freq);this.drawFrame(audio.currentTime,duration,bands(freq),project);onProgress(unit(audio.currentTime/duration));raf=requestAnimationFrame(loop)};loop();while(!ended&&!this.abortFlag)await sleep(120);cancelAnimationFrame(raf);if(!audio.paused)audio.pause();if(rec.state!=='inactive')rec.stop();await stopped;await write;if(writeError)throw writeError;if(this.abortFlag)throw new Error('Render stopped safely.');const file=writer?await finishOpfs(writer):new File(chunks,name,{type:mime||'video/webm'});onProgress(1);return {file,mime:file.type||mime||'video/webm',quality:q,duration,sceneCount:project.scenePlan?.scenes?.length||0}
    }catch(e){if(writer)try{await writer.w.abort()}catch{}throw e}
    finally{if(visibilityHandler)document.removeEventListener('visibilitychange',visibilityHandler);cancelAnimationFrame(raf);stream?.getTracks().forEach(t=>t.stop());try{audio?.pause()}catch{}try{await ac?.close()}catch{}if(ownsUrl&&url)try{URL.revokeObjectURL(url)}catch{}this._releaseRenderAudio();this.lease.clear();chunks=[];this.rendering=false}
  }
  close(){this.stopPreview(true);this.lease.close();this._releaseRenderAudio();try{this.preview?.ac?.close()}catch{}this.preview=null;try{this.gestureCtx?.close()}catch{}this.gestureCtx=null}
}
export {bands};